# CLAUDE.md — 最上位ルーター（道案内）

> このファイルは**道案内専用**です。具体的な規約・手順は書きません。
> あなた（AI）は、タスクに応じて下記フローで**必要なルールだけを自律的にロード**してください。

## 🧭 ロード手順（必ずこの順で辿ること）

1. **起点を開く**: [.claude/rules/index.md](.claude/rules/index.md) を開き、今のタスクが属するシーンを 1 つ（または複数）選ぶ。
2. **カタログを辿る**: 選んだシーンの `index.md` を開く。各フォルダは直下に `index.md`（カタログ）を持ち、親は子 `index.md` へ委譲する。必要な深さ（プラットフォーム → framework 等）まで `index.md` を辿る。
3. **ケースだけ開く**: **タスクに関係するケース Markdown だけ**を開いて内容に従う。
4. 関係ないシーン・ケースは**読み込まない**（コンテキストを汚さない）。

> 各シーン・各カタログの一覧は [.claude/rules/index.md](.claude/rules/index.md) を起点に辿れる。迷ったら、まず該当しそうなカタログを開いて概要を読むこと。

## 🛠️ スキル

再利用可能な作業手順は `.claude/skills/` に**スキル**として定義されています。
スキルは各 `SKILL.md` の `description` に基づき**必要な場面で自動的に呼び出される**ため、rules のように手動でルーティングする必要はありません（ここに一覧を書かない）。作業内容に合致するスキルがあれば起動してください。

## ⚠️ このルーターの鉄則

- ここに**規約の中身を書かない**。すべて `.claude/rules/` のカタログ／ケースへ。
- **シーンの追加は `.claude/rules/index.md`** で行う。CLAUDE.md（このファイル）は起点への誘導だけで、原則いじらない。
- 迷ったら「起点 → カタログ → ケース」の順を崩さない。
